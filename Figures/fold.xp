#include "epix.h"
#include "stdio.h"

#define delta 0.12

using namespace ePiX;

// double ux (double x) {  return (1./3.*x*x + x*x*x/6. + x*x*x*x/30.) ;}


double branchplus (double psi){ return (sqrt(-psi));}
double branchminus (double psi){ return (-sqrt(-psi));}

int main() {

  bounding_box(P(-1.2,-1.4), P(1.2,1.4));
  picture(2.4,2.8);
  unitlength("2cm");

  begin();

  arrow(P(0,-1.3), P(0,1.3));
  arrow(P(-1.,0), P(1.,0));

  label(P(0,1.4), P(5,0),"$x^\\prime$", r);
  label(P(1.1,0), P(5,0),"$\\psi^\\prime$", l);

  bbold();
  solid();
  plot(branchminus, -1.00, 0., 40);
  dashed();
  plot(branchplus, -1.00, 0., 40);

  tikz_format();
  end();
}
